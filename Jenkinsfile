pipeline {
    agent { docker { image 'php' } }
    stages {
        stage('prep') {
            steps {
                sh 'php --version'
            }
        }
        stage('build') {
            steps {
                sh 'php --version'
            }
        }
        stage('test') {
            steps {
                sh 'php --version'
            }
        stage('deploy') {
            steps {
                sh 'php --version'
            }
            
        }
    }
}
